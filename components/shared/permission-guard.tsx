/**
 * Backward-compat alias — يستخدم ActionGuard الجديد تحت الغطاء.
 * استخدم `ActionGuard` مباشرة من `@/components/permissions` في الكود الجديد.
 */
export { ActionGuard as PermissionGuard } from "@/components/permissions/action-guard";
